
setup-job: # This job runs in the setup stage.
  - stage: setup
    script:
      - rm -rf node_modules
      - npm i -g next
      - echo "Installing dependencies..."
      - yarn install

  - stage: build
    script:
      - echo "Building the project..."
      - yarn build
      - echo "Build complete."

